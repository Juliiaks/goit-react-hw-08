import { LoginForm } from "../components/LoginForm/LoginForm";

export default function LoginPage(paras) {
    
//     const dispach = useDispatch();
    
//     const login = (userData) => {
// //  dispach(registerOperation(userData))}
   
    return (
        <div>
            <LoginForm/>
        </div>
    )
}